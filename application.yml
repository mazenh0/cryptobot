server:
port: 8080
spring:
codec:
max-in-memory-size: 10MB
management:
endpoints:
web:
exposure:
include: health,info,prometheus
metrics:
tags:
application: trading-bot
app:
ws:
binance: wss://stream.binance.com:9443/ws # placeholder; adapter simulates unless enabled
coinbase: wss://ws-feed.exchange.coinbase.com # placeholder; adapter simulates unless enabled
security:
api-key: change-me